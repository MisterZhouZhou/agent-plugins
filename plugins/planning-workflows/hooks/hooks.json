{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "PLUGIN_DIR=\"${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}\"; python3 \"$PLUGIN_DIR/hooks/session_start.py\"",
            "commandWindows": "python -c \"import os,runpy; root=os.environ.get('CLAUDE_PLUGIN_ROOT') or os.environ['PLUGIN_ROOT']; runpy.run_path(os.path.join(root, 'hooks', 'session_start.py'), run_name='__main__')\"",
            "statusMessage": "Loading planning workflow rules"
          }
        ]
      }
    ]
  }
}
