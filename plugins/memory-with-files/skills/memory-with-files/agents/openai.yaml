interface:
  display_name: "Memory With Files"
  short_description: "Persist project context without managing tasks"
  default_prompt: "Use $memory-with-files to preserve this project's durable context under its root .memory directory; never use global Codex memories."
