# 参考上下文

<项目代码上下文：用户未提供时保留此占位标签，由用户后续补充>

- popup 组件参考路径：<popup组件参考路径>
- radio 组件参考路径：<radio组件参考路径>
- 接口文件位置：<接口文件位置>
- 主题色变量：<主题色样式变量>
- 其他通用约束：<其他项目约束>

# 功能需求

> 二级标题按"页面 / 弹窗 / 公共组件"维度组织，不要复用 PRD 章节号。
> PRD 中散落在各章节的规则（字段、校验、状态、显隐、跳转、文案）按"哪个页面用得到"分发到对应页面下。
> 综合 PRD + 设计稿提炼：有正式设计稿时，页面样式与页面功能均以设计稿为准（PRD/原型是方案意图，设计稿是最终要实现的效果）；无设计稿再用原型。跨页业务规则（准入、状态机语义、金额口径、互斥）仍从 PRD 提炼并贴到对应页面。
> `设计稿：` 优先填该页最具体的 Figma 子链接（含 node-id）。PRD 只有完整功能模版总入口时，须做帧↔页面映射后写入各页，禁止各页统一占位只挂一个总入口。帧名与画面不一致时以画面为准。
> 映射不到或确实没有设计稿时，才用占位：<设计稿地址：xxx场景>。页面创建路径、接口路径、字段命名等同理：<页面创建路径>、<接口路径>、<字段名>。
> 原文档已经明确的设计链接、项目代码路径和业务字段含义必须原样保留；后端接口路径、请求参数名和响应字段名除非来自用户明确提供的真实接口文档或命名规则，否则仍使用占位。

## 接口与 Mock 约束

- 真实接口文档未提供时，先在独立 API 层提供 Mock 实现；页面和组件只能通过统一 API 方法获取或提交数据，禁止直接写死 Mock 业务数据。
- Mock 与真实接口保持相同的 Promise/异步调用方式，并完整处理 loading、成功、空数据和失败状态。
- 后续接入真实接口时，原则上只替换 API 实现、请求参数和响应字段映射，不修改页面交互和业务流程。
- 未确认的接口路径、请求参数、响应字段和后端命名使用 `<接口路径>`、`<请求参数>`、`<响应字段>`、`<字段名>` 占位，不臆造真实接口契约。

## 公共组件

### <组件名1>

1. 组件定位：
   - 路径：<组件创建路径，例如 src/views/xxx/components/xxx>
   - 用途：<一句话说明用途>
   - 设计稿：<设计稿地址：组件主态>，UI 需 `1:1` 还原

2. 结构与展示：
   - <结构条目1>
   - <结构条目2>

3. 字段与校验：
   - <字段1>：数据类型 <类型> | 使用组件 <input/select/radio/checkbox/datepicker/upload/自定义组件> | <必填/选填> | 校验规则 <规则> | 数据来源 <来源> | <只读/可编辑>
   - <字段2>：数据类型 <类型> | 使用组件 <组件> | <必填/选填> | 校验规则 <规则> | 数据来源 <来源> | <只读/可编辑>
   - 字段级联规则：<级联条件>

4. 交互：
   - 点击 `<按钮1>`：<行为，含跳转目标 / 弹窗 / 接口>
   - 点击 `<按钮2>`：<行为，含跳转目标 / 弹窗 / 接口>
   - 弹窗形态切换：<编辑态/查看态/新增态切换规则>

5. 接口占位：
   - <动作1>：通过独立 API 方法调用 <接口路径>；真实接口未提供时由 API 层返回 Mock 数据，页面按真实异步流程处理 loading、成功、空数据和失败状态；后续仅替换 API 实现和字段映射
   - <动作2>：通过独立 API 方法调用 <接口路径>；禁止在组件内直接写死 Mock 数据，成功后执行 <成功行为>，失败时执行 <失败行为>

## <一级页面分组1，例如 "开票信息"、"我的开票"，可省略此层级>

### <页面1，例如 单张开发票页面>

1. 页面定位：
   - 路径：<页面创建路径>
   - 路由：在 <路由模块路径> 添加路由并在 <路由总入口> 中引用
   - 设计稿：<设计稿地址：主态>，UI 需 `1:1` 还原
   - 导航：<原生 Native 导航 / 自定义导航 / 通过 document.title 设置标题>

2. 进入条件：
   - <从哪个入口进入>
   - <前置数据要求>

3. 页面结构与字段：
   - <区域1>：<展示内容、是否只读、数据来源>
   - <区域2>：<展示内容、是否只读、数据来源>
   - 字段级联 / 显隐规则：<规则>
   - 校验规则：<字段校验规则>

4. 交互：
   - 选择 / 输入：<选中后状态变化、汇总计算等>
   - 底部按钮 `<按钮1>`：<行为>
   - 底部按钮 `<按钮2>`：通过独立 API 方法调用 <接口路径>；真实接口未提供时由 API 层返回 Mock 数据，页面按真实异步流程处理 loading、成功和失败状态；成功后跳转 `<目标页面>`，后续仅替换 API 实现和字段映射

5. 异常态：
   - 缺省页：<主标题>，描述 <描述文案，含换行规则>
   - 空数据 / 加载失败：<行为>

6. 自动行为：
   - 下拉刷新 / 上拉加载：<是否支持，参考 <参考实现路径> 的 list-refresh 用法>
   - 其他自动逻辑：<自动计算 / 自动填充 / 自动跳转>

### <页面2>

1. 页面定位：
   - 路径：<页面创建路径>
   - 路由：在 <路由模块路径> 添加路由并在 <路由总入口> 中引用
   - 设计稿：<设计稿地址：主态>，UI 需 `1:1` 还原

2. 状态分支（如该页面承载多种状态/类型）：
   - <类型A> + <状态1>：<设计稿地址：A-1>
   - <类型A> + <状态2>：<设计稿地址：A-2>
   - <类型A> + <状态3>：<设计稿地址：A-3>
   - <类型B> + <状态1>：<设计稿地址：B-1>
   - <类型B> + <状态2>：<设计稿地址：B-2>
   - <类型B> + <状态3>：<设计稿地址：B-3>

3. 页面结构与字段：
   - <区域1>：<展示内容>
   - <区域2>：<展示内容>

4. 交互：
   - 点击 `<按钮>`：根据 <判断条件> 跳转不同页面
     - <条件A>：跳转 `<目标页面A>`
     - <条件B>：跳转 `<目标页面B>`

5. 接口占位：
   - <动作>：通过独立 API 方法调用 <接口路径>；真实接口未提供时由 API 层返回 Mock 数据，禁止在页面内直接写死业务数据；后续仅替换 API 实现和字段映射

## 工作流程

- 先不要着急开发，你先尝试理解需求，有任何问题都可以问我
- 待我确定无误后再进行任务拆分
- 我确定任务无误后再进行开发
